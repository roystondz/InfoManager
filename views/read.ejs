<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900">
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <a class="text-blue-500" href="/" >Back to home</a>
        <% if(user.length > 0) { %>
        <h1 class="text-3xl tracking-tighter mb-3 mt-5">All users</h1>

        <div class="users flex gap-3 flex-wrap">
            <% user.forEach(function(user) { %>
                <div class="user w-72 p-4 bg-zinc-800 rounded-lg shadow-md">
                    <div class="w-full h-52 rounded-lg  bg-zinc-500 overflow-hidden"> <img class=" w-full h-full object-cover object-top" src="<%=user.image %>"></div>
                    <h3 class="text-xl tracking-tighter mt-3"><%= user.name %></h3>
                    <h5 class="text-zinc-500"><%= user.email %></h5>
                    <div class="flex gap-3 mt-4">
                        <a class="text-zinc-600" href="/edit/<%=user._id%>">Edit user</a>
                        <a class="text-red-600" href="/delete/<%=user._id%>">Delete user</a>
                    </div>
                </div>

             <%})%>   
        </div>
        <% } else { %>
            <h2 class="text-white">No users found</h2>
        <% }%>
    </div>
</body>
</html>